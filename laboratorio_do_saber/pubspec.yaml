name: laboratorio_do_saber
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
  mobile_scanner: ^7.0.1
  mask_text_input_formatter: ^2.9.0
  url_launcher: ^6.3.2
  flutter_local_notifications: ^19.4.1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
